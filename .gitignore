# Variables de entorno (NUNCA subir a git con credenciales reales)
.env
.env.local
.env.production

# Node modules
node_modules/
**/node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporales / builds
dist/
build/
front-pos/build/

# Sistema
.DS_Store
Thumbs.db

# BD dumps / datos
estructura.sql
backup.sql
backend/migrations/datos_iniciales.sql
backend/backups/*.json
backend/backups/papepos_full_*.sql
# NOTA: solo papepos_full.sql (sin timestamp) se versiona para migrar

# Uploads: las imagenes SÍ se versionan por ahora (son pocas y pequeñas)
# Si crecen mucho en el futuro, descomentar:
# backend/uploads/productos/*
# !backend/uploads/productos/.gitkeep

# Editor
.vscode/
.idea/
*.swp
